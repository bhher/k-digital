# 배포 및 업데이트 가이드

이 문서는 웹 애플리케이션을 배포하고, 이후 업데이트를 자동으로 배포하는 방법을 단계별로 설명합니다.

## 현재 저장소 정보

- **원격 저장소**: https://github.com/bhher/k-digital.git
- **현재 브랜치**: main
- **프로젝트 타입**: Vite + React + TypeScript

---

## ⚡ 빠른 시작 (5분 안에 배포하기)

### 가장 빠른 방법: Vercel 사용

1. **[Vercel.com](https://vercel.com) 접속** → GitHub로 로그인
2. **"Add New Project"** 클릭
3. **`bhher/k-digital` 저장소 선택** → Import
4. **"Deploy" 버튼 클릭**
5. **완료!** 배포된 URL 확인

이제부터는 코드 수정 후 `git push`만 하면 자동으로 배포됩니다!

```bash
# 업데이트 및 배포 (3단계)
git add .
git commit -m "변경사항 설명"
git push  # 자동 배포 시작!
```

---

## 📋 배포 옵션 비교

| 옵션 | 장점 | 단점 | 추천도 |
|------|------|------|--------|
| **Vercel** | 설정 간단, 자동 배포, 무료 | - | ⭐⭐⭐⭐⭐ |
| **GitHub Pages** | 무료, GitHub 통합 | 설정 복잡 | ⭐⭐⭐⭐ |
| **Netlify** | 설정 간단, 자동 배포 | - | ⭐⭐⭐⭐ |

---

## 🚀 방법 1: Vercel 배포 (가장 추천)

Vercel은 가장 간단하고 빠른 배포 방법입니다. GitHub에 푸시하면 자동으로 배포됩니다.

### 1단계: Vercel 계정 생성 및 프로젝트 연결

1. [Vercel 웹사이트](https://vercel.com) 접속
2. "Sign Up" 클릭 → GitHub 계정으로 로그인
3. "Add New Project" 클릭
4. GitHub 저장소 `bhher/k-digital` 선택
5. "Import" 클릭

### 2단계: 빌드 설정

Vercel이 자동으로 Vite 프로젝트를 감지하지만, 확인해봅시다:

- **Framework Preset**: Vite
- **Build Command**: `npm run build`
- **Output Directory**: `dist`
- **Install Command**: `npm install`

### 3단계: 환경 변수 설정 (필요한 경우)

`.env.local` 파일에 있는 환경 변수가 있다면:

1. Vercel 대시보드에서 프로젝트 선택
2. "Settings" → "Environment Variables" 클릭
3. 변수 추가 (예: `GEMINI_API_KEY`)

### 4단계: 배포 시작

1. "Deploy" 버튼 클릭
2. 배포 완료까지 1-2분 대기
3. 배포된 URL 확인 (예: `https://k-digital.vercel.app`)

### 5단계: 자동 배포 확인

이제부터는 **GitHub에 푸시만 하면 자동으로 배포**됩니다!

```bash
# 1. 코드 수정
# 2. 변경사항 커밋 및 푸시
git add .
git commit -m "기능 업데이트"
git push

# 3. Vercel이 자동으로 배포 시작 (약 1-2분 소요)
```

---

## 🌐 방법 2: GitHub Pages 배포

GitHub Pages는 무료이지만 설정이 조금 복잡합니다.

### 1단계: Vite 설정 수정

`vite.config.ts` 파일을 수정하여 GitHub Pages용 base path를 설정합니다.

```typescript
import path from 'path';
import { defineConfig, loadEnv } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig(({ mode }) => {
    const env = loadEnv(mode, '.', '');
    return {
      base: '/k-digital/', // 저장소 이름과 동일하게 설정
      server: {
        port: 3000,
        host: '0.0.0.0',
      },
      plugins: [react()],
      define: {
        'process.env.API_KEY': JSON.stringify(env.GEMINI_API_KEY),
        'process.env.GEMINI_API_KEY': JSON.stringify(env.GEMINI_API_KEY)
      },
      resolve: {
        alias: {
          '@': path.resolve(__dirname, '.'),
        }
      }
    };
});
```

### 2단계: GitHub Actions 워크플로우 생성

`.github/workflows/deploy.yml` 파일을 생성합니다.

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

### 3단계: GitHub Pages 설정

1. GitHub 저장소 페이지로 이동
2. "Settings" → "Pages" 클릭
3. "Source"에서 "GitHub Actions" 선택
4. 저장

### 4단계: 환경 변수 설정 (필요한 경우)

1. GitHub 저장소 → "Settings" → "Secrets and variables" → "Actions"
2. "New repository secret" 클릭
3. 이름: `GEMINI_API_KEY`, 값: API 키 입력
4. "Add secret" 클릭

### 5단계: 배포 트리거

```bash
# 코드 수정 후 푸시하면 자동 배포됩니다
git add .
git commit -m "GitHub Pages 배포 설정"
git push
```

### 6단계: 배포 확인

1. GitHub 저장소 → "Actions" 탭에서 배포 진행 상황 확인
2. 배포 완료 후 `https://bhher.github.io/k-digital/` 접속

---

## 🔄 업데이트 및 재배포 프로세스

### 자동 배포가 설정된 경우

**Vercel 또는 GitHub Actions를 사용하면 자동 배포됩니다!**

```bash
# 1. 로컬에서 코드 수정
# 예: App.tsx 파일 수정

# 2. 변경사항 확인
git status

# 3. 변경사항 스테이징
git add .

# 4. 커밋 생성
git commit -m "UI 개선 및 버그 수정"

# 5. GitHub에 푸시
git push origin main

# 6. 자동 배포 시작 (약 1-2분 소요)
# - Vercel: 대시보드에서 배포 상태 확인
# - GitHub Pages: Actions 탭에서 배포 상태 확인
```

### 수동 배포가 필요한 경우

만약 자동 배포가 설정되지 않았다면:

```bash
# 1. 프로젝트 빌드
npm run build

# 2. 빌드 결과 확인
# dist 폴더에 빌드된 파일들이 생성됩니다

# 3. 배포 플랫폼에 업로드
# (플랫폼별 방법이 다름)
```

---

## 📝 단계별 체크리스트

### 초기 배포 체크리스트

#### Vercel 사용 시
- [ ] Vercel 계정 생성 및 GitHub 연결
- [ ] 프로젝트 import
- [ ] 빌드 설정 확인
- [ ] 환경 변수 설정 (필요한 경우)
- [ ] 첫 배포 완료 확인
- [ ] 배포된 URL 테스트

#### GitHub Pages 사용 시
- [ ] `vite.config.ts`에 base path 설정
- [ ] `.github/workflows/deploy.yml` 파일 생성
- [ ] GitHub Pages 설정 (Settings → Pages)
- [ ] 환경 변수 설정 (Secrets)
- [ ] 첫 배포 완료 확인
- [ ] 배포된 URL 테스트

### 업데이트 체크리스트

- [ ] 로컬에서 코드 수정
- [ ] `git status`로 변경사항 확인
- [ ] `git add .`로 스테이징
- [ ] `git commit -m "변경사항 설명"`로 커밋
- [ ] `git push`로 GitHub에 푸시
- [ ] 배포 플랫폼에서 배포 상태 확인
- [ ] 배포 완료 후 사이트 테스트

---

## 🔍 배포 상태 확인 방법

### Vercel
1. [Vercel 대시보드](https://vercel.com/dashboard) 접속
2. 프로젝트 선택
3. "Deployments" 탭에서 배포 상태 확인
4. 각 배포의 상세 정보 및 로그 확인

### GitHub Pages
1. GitHub 저장소 → "Actions" 탭
2. "Deploy to GitHub Pages" 워크플로우 클릭
3. 최신 실행 내역에서 배포 상태 확인
4. 로그를 통해 오류 확인

---

## 🛠️ 빌드 및 배포 명령어

### 로컬 빌드 테스트

```bash
# 개발 서버 실행
npm run dev

# 프로덕션 빌드
npm run build

# 빌드 결과 미리보기
npm run preview
```

### 빌드 결과 확인

```bash
# 빌드 후 dist 폴더 확인
ls dist

# 빌드된 파일들:
# - index.html
# - assets/ (JS, CSS 파일들)
```

---

## ⚠️ 주의사항 및 문제 해결

### 1. 빌드 오류

**문제**: `npm run build` 실행 시 오류 발생

**해결 방법**:
```bash
# node_modules 재설치
rm -rf node_modules
npm install

# 캐시 클리어
npm run build -- --force
```

### 2. 배포 후 404 오류

**문제**: 배포 후 페이지가 표시되지 않음

**해결 방법**:
- GitHub Pages: `vite.config.ts`의 `base` 경로 확인
- Vercel: 프로젝트 설정에서 빌드 출력 디렉토리가 `dist`인지 확인

### 3. 환경 변수 누락

**문제**: 배포된 사이트에서 API 키가 작동하지 않음

**해결 방법**:
- Vercel: Settings → Environment Variables에서 변수 추가
- GitHub Pages: Settings → Secrets and variables → Actions에서 변수 추가

### 4. 자동 배포가 작동하지 않음

**문제**: 푸시해도 배포가 시작되지 않음

**해결 방법**:
- Vercel: 대시보드에서 프로젝트가 연결되어 있는지 확인
- GitHub Pages: Actions 탭에서 워크플로우가 활성화되어 있는지 확인
- GitHub 저장소 → Settings → Actions → General에서 Actions가 활성화되어 있는지 확인

---

## 🎯 권장 워크플로우

### 일일 작업 흐름

```bash
# 1. 작업 시작 전
git pull  # 최신 코드 가져오기

# 2. 코드 수정 및 개발
npm run dev  # 개발 서버 실행하며 작업

# 3. 작업 완료 후
git status  # 변경사항 확인
git add .   # 스테이징
git commit -m "작업 내용 설명"  # 커밋
git push    # 푸시 및 자동 배포

# 4. 배포 확인
# Vercel 또는 GitHub Actions에서 배포 상태 확인
```

### 배포 전 확인사항

- [ ] 로컬에서 `npm run build` 성공
- [ ] `npm run preview`로 빌드 결과 확인
- [ ] 주요 기능 테스트
- [ ] 환경 변수 설정 확인
- [ ] 커밋 메시지 명확하게 작성

---

## 📚 추가 리소스

### 공식 문서
- [Vercel 문서](https://vercel.com/docs)
- [GitHub Pages 문서](https://docs.github.com/pages)
- [Vite 배포 가이드](https://vitejs.dev/guide/static-deploy.html)

### 유용한 링크
- [Vercel 대시보드](https://vercel.com/dashboard)
- [GitHub Actions](https://github.com/features/actions)

---

## 💡 팁

1. **자동 배포 활용**: Vercel이나 GitHub Actions를 사용하면 푸시만으로 자동 배포됩니다
2. **프리뷰 배포**: Vercel은 Pull Request마다 프리뷰 URL을 자동 생성합니다
3. **환경 변수 관리**: 민감한 정보는 환경 변수로 관리하세요
4. **빌드 테스트**: 배포 전 로컬에서 빌드 테스트를 꼭 하세요
5. **배포 로그 확인**: 배포 실패 시 로그를 확인하여 문제를 파악하세요

---

## 🎉 완료!

이제 다음 명령어만으로 업데이트와 배포가 가능합니다:

```bash
git add .
git commit -m "변경사항 설명"
git push
```

**자동 배포가 설정되어 있다면, 푸시 후 1-2분이면 배포가 완료됩니다!**

---

**마지막 업데이트**: 2024년

